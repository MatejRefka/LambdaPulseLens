apiVersion: 1

datasources:
  - uid: loki
    name: Loki
    type: loki
    access: proxy ##user access to loki via grafana proxy
    url: http://loki:3100
    isDefault: true ##default datasource
    editable: true