worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    sendfile on;
    server_tokens off;

    server {
        listen 80;
        server_name lambdapulse.app www.lambdapulse.app;

        location ^~ /.well-known/acme-challenge/ {
            root /var/www/certbot;
            default_type text/plain;
            try_files $uri =404;
        }

        location / {
            return 301 https://lambdapulse.app$request_uri;
        }
    }

    server {
        listen 443 ssl;
        http2 on;
        server_name www.lambdapulse.app;

        ssl_certificate /etc/letsencrypt/live/lambdapulse.app/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/lambdapulse.app/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_session_cache shared:SSL:10m;
        ssl_session_timeout 1d;
        ssl_session_tickets off;

        return 301 https://lambdapulse.app$request_uri;
    }

    server {
        listen 443 ssl;
        http2 on;
        server_name lambdapulse.app;

        ssl_certificate /etc/letsencrypt/live/lambdapulse.app/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/lambdapulse.app/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_session_cache shared:SSL:10m;
        ssl_session_timeout 1d;
        ssl_session_tickets off;

        location / {
            proxy_pass http://lens:8080;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            proxy_buffering off;
            proxy_read_timeout 1h;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
        }
    }
}
